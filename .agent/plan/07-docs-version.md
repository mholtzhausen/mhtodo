# 07 — Docs and version

- Bump VERSION to 0.3.0 (Makefile VERSION)
- README agent contract: parent_id, review, activity commands, list columns note
- AGENTS.md scope: v0.3 features in scope
- Regenerate wails bindings; `make test` && `make build`
